<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1">


    <application
        android:name=".LoginText"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AlarmActivity"></activity>
        <activity android:name=".MypageActivity" />
        <activity android:name=".GPSparkActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".FriendhomeActivity" />
        <activity android:name=".ROAhomeActivity" />
        <activity android:name=".GPSnowActivity" />
        <activity android:name=".GPShomeActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/actions1" />

        <activity android:name=".MenuActivity" />
        <activity android:name=".SignActivity" />
        <activity android:name=".FindPassActivity" />
        <activity android:name=".FindIdActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

</manifest>